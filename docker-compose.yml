version: "3.9"

services:
  app:
    build:
      context: .
      args:
        - DEV=true
    ports:
      - "8000:8000"  # Change the host port to avoid conflicts
    
    volumes:
      - ./app:/app  # Map in real-time
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"
